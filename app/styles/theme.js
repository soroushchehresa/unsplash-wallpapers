import theme from 'styled-theming';

export const boxsBackgroundColor = theme('mode', {
  Light: '#efefef',
  Dark: '#3a3e3f',
});

export const textsColor = theme('mode', {
  Light: '#222',
  Dark: '#fff',
});
